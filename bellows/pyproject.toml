[project]
name = "bellows"
version = "0.2.0"
description = "Bellows — manage multiple Forge projects from your browser"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "jinja2>=3.1.0",
    "python-multipart>=0.0.24",
]
