# Feedback

Human writes feedback here. Forge reads it at the start of each run and generates fix tasks.

## 2026-04-10

- Decision cards need to be actionable — tap to approve/reject inline, not just display
- Direct tab quick actions are too generic — should show project-specific actions
- Heat timeline in Activity tab needs expandable detail (collapsed by default, tap to expand)
- Auto-refresh would be nice during active runs — currently need to manually reload
- The lifecycle-adaptive cards are great — keep that pattern
- Budget bar on Direct tab should update when "add heats" is clicked (currently requires reload)
