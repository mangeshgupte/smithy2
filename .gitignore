# Forge transient files (per-heat locks + scratch)
.forge-checkpoint*.json
.forge-handoff.json
.forge-output.log

# t-425: append-only rig telemetry — local to each clone
rig-events.jsonl

# Local workspace
.worktrees/

# Python packaging
smithy/smithy.egg-info/

# t-461 (ini-020 phase 2): per-Forge / per-worktree uv venvs
.venv/
**/.venv/

# Runtime coordination artifacts — local, not version-tracked
.smithy-nudge-queue/
.state.json.lock

# Assembly runtime queue + restart backups + editor config — local only
.assembly-queue.jsonl
.pre-restart-backups/
.obsidian/

# t-533: per-rejection full-test-output dumps. Reject reason in
# state.json carries the file:// pointer; files themselves are local-only.
.assembly-logs/

# t-563: plumbing-rejected queue rows preserved for recovery.
.assembly-queue-dead.jsonl
